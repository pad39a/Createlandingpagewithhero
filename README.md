
  # Create landing page with hero

  This is a code bundle for Create landing page with hero. The original project is available at https://www.figma.com/design/rgpJthSCPVwy7TLo6ewNgW/Create-landing-page-with-hero.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  